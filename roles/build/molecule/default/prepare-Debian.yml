---
- name: Install Docker
  become: true
  ansible.builtin.apt:
    name:
      - docker.io
    state: present

- name: Add user to docker group
  become: true
  ansible.builtin.user:
    name: vagrant
    groups:
      - docker
    append: true
  notify:
    - _reboot
