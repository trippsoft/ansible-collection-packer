---
name: Generate Role Docs
'on':
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - galaxy.yml
      - roles/build/.aar_doc.yml
      - roles/build/README.md
      - roles/build/meta/argument_specs.yml
      - roles/build/meta/main.yml
      - roles/install/.aar_doc.yml
      - roles/install/README.md
      - roles/install/meta/argument_specs.yml
      - roles/install/meta/main.yml
defaults:
  run:
    working-directory: 'trippsc2.packer'
jobs:
  docs_build:
    name: Generate build role documentation
    permissions:
      contents: write
    runs-on:
      - self-hosted
      - linux
      - ansible
      - x64
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          path: trippsc2.packer
          ref: main
      - name: Generate build role documentation
        working-directory: 'trippsc2.packer'
        run: |
          source ~/venv/ansible-2.16/bin/activate
          cd ./roles/build
          aar-doc . markdown
          git config user.name "GitHub Actions"
          git config user.email "github-actions@github.com"
          git add README.md || /bin/true
          git commit -m "Update build role documentation." || /bin/true
          git push origin main
          deactivate
  docs_install:
    name: Generate install role documentation
    needs: docs_build
    permissions:
      contents: write
    runs-on:
      - self-hosted
      - linux
      - ansible
      - x64
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          path: trippsc2.packer
          ref: main
      - name: Generate install role documentation
        working-directory: 'trippsc2.packer'
        run: |
          source ~/venv/ansible-2.16/bin/activate
          cd ./roles/install
          aar-doc . markdown
          git config user.name "GitHub Actions"
          git config user.email "github-actions@github.com"
          git add README.md || /bin/true
          git commit -m "Update install role documentation." || /bin/true
          git push origin main
          deactivate
